export function SiteFooter() {
  return (
    <footer className="border-t border-black/10 py-10">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-3 px-4 text-sm text-[var(--color-muted)] md:px-8">
        <p>ITA Event Planner UI shell for future microservices integration.</p>
        <p>Designed for events, applications, and users service boundaries.</p>
      </div>
    </footer>
  );
}
